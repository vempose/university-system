package university;

import university.tui.TuiApplication;

/// Entry point of the university system.
///
/// Just launches the TUI application. Nothing fancy.
public class Main {

    /// Launches the TUI.
    void main() {
        TuiApplication.main(new String[0]);
    }
}

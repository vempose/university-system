package university.comparator;

import university.domain.user.User;

import java.util.Comparator;

/// Common interface for all user comparators.
public interface UserComparator extends Comparator<User> {
}

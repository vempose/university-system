package university.enums;

/// Roles a student can hold within a `StudentOrganization`.
///
/// Simple two-level hierarchy: regular members and the head.
public enum OrganizationRole {
    /// Regular member
    MEMBER,
    /// President / leader
    HEAD,
}
